const width = {
  // desktop :
  laptop: '450px',
  // tablet :
  // mobile :
};

const height = {
  // desktop :
  laptop: '800px',
  // tablet :
  // mobile :
};

const theme = {
  laptopWidth: `${width.laptop}`,
  laptopHeight: `${height.laptop}`,
};

export default theme;
